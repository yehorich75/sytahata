mixin food-menu(src, alt, title, link)
    a.grid__item(href="#")
        .grid__item-bg
        .grid__item-wrap
            img(src= src alt= alt).grid__item-img/
        h3.grid__item-title= title
            //- a(href= link).btn.btn-secondary.text-bold.text-uppercase.animation.food-menu__more Докладніше